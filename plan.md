# Plan: prj-3566-task-mugr7azh

**Project:** `prj-3566-task-mugr7azh`  
**Task ID:** `task-mugr7azh`  
**Repo:** `prj-3566-task-mugr7azh`  

## Summary

Декомпозирую задачу на 3 этапа: проектирование CLI и структуры, реализация модуля сжатия с поддержкой форматов, юнит-тесты и валидация. Каждый этап проверяем идемпотентно.

## Subtasks

### 1. Спроектировать CLI-скелет и структуру проекта

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать репозиторий prj-3566-task-mugr7azh: README.md, requirements.txt, cli.py (точка входа argparse с subcommands compress/decompress), модуль core/ с интерфейсами компрессора. Определить поддерживаемые форматы (gzip, zip, tar.gz).
- **Test plan:** Запустить python cli.py --help и python cli.py compress --help — справка отображается без ошибок, subcommands видны. requirements.txt валиден (pip install -r проходит).

### 2. Реализовать модули сжатия и распаковки

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать core/compressor.py с функциями compress_file(src, dst, fmt) и decompress_file(src, dst, fmt). Поддержка gzip, zip, tar.gz. Добавить валидацию входных файлов, обработку ошибок (файл не найден, неподдерживаемый формат). Покрыть type hints.
- **Test plan:** Создать тестовый файл, выполнить compress + decompress round-trip, проверить идентичность содержимого через sha256sum. Негативные кейсы: несуществующий файл → понятная ошибка, неверный формат → понятная ошибка.
- **Dependencies:** `sub-1`

### 3. Тесты и валидация CLI

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить tests/test_compressor.py с pytest-кейсами: round-trip для каждого формата, обработка ошибок, тесты CLI через subprocess. README.md дополнить примерами использования. Прогнать pytest -v и убедиться что все тесты зелёные.
- **Test plan:** pytest -v возвращает все тесты PASSED. CLI принимает реальные файлы: python cli.py compress input.txt output.gz и обратная операция восстанавливает исходник.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
